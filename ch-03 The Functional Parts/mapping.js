const numbers = [1, 2, 3 ,4, 5];

const doubledNumbers = numbers.map(x => x * 2);     //alternate to using for loop and index. in FP we are concerned with WHAT? not HOW?

console.log(doubledNumbers);
